apiVersion: apps/v1
kind: Deployment
metadata:
  name: feeds-microservice
  namespace: microservices
  labels:
    app.kubernetes.io/name: feeds-microservice
    app.kubernetes.io/component: application
spec:
  replicas: 1
  selector:
    matchLabels:
      app: feeds-microservice
  template:
    metadata:
      labels:
        app: feeds-microservice
    spec:
      containers:
        - name: feeds-microservice
          image: ghcr.io/archisman-mridha/feeds-microservice:latest
          ports:
            - containerPort: 4000
          resources:
            limits:
              memory: 256Mi
            requests:
              cpu: 200m
              memory: 256Mi
          volumeMounts:
            - name: config
              mountPath: /var/feeds-microservice/config.yaml
              readOnly: true
              subPath: config.yaml
          livenessProbe:
            grpc:
              port: 4000
          readinessProbe:
            grpc:
              port: 4000
      volumes:
        - name: config
          secret:
            secretName: feeds-microservice-config
