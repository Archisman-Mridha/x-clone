apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-microservice
  namespace: microservices
  labels:
    app.kubernetes.io/name: users-microservice
    app.kubernetes.io/component: application
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users-microservice
  template:
    metadata:
      labels:
        app: users-microservice
    spec:
      containers:
        - name: users-microservice
          image: ghcr.io/archisman-mridha/users-microservice:latest
          ports:
            - containerPort: 4000
          resources:
            limits:
              memory: 256Mi
            requests:
              cpu: 200m
              memory: 256Mi
          volumeMounts:
            - name: config
              mountPath: /var/users-microservice/config.yaml
              readOnly: true
              subPath: config.yaml
          livenessProbe:
            grpc:
              port: 4000
          readinessProbe:
            grpc:
              port: 4000
      volumes:
        - name: config
          secret:
            secretName: users-microservice-config
